CREATE TABLE IF NOT EXISTS mod_entrants (`id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY, `char` VARCHAR(20) NOT NULL, `bot` VARCHAR(20) NOT NULL, `sponsor` VARCHAR(20) NOT NULL, `org` VARCHAR(75) NOT NULL);