CREATE TABLE IF NOT EXISTS mod_history (`id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY, `time` INT NOT NULL, `mod` VARCHAR(20) NOT NULL, `bot` VARCHAR(20) NOT NULL, `sponsor` VARCHAR(20) NOT NULL, `org` VARCHAR(75), `votes` INT);